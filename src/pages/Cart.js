import { Typography } from '@mui/material';
import React from 'react'

export default function Cart() {
  return (
    <Typography component={'div'} height={'90vh'}  color={"red"} variant="h3" textAlign={"center"}>
      cart
    </Typography>
  );
}
